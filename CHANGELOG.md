# 변경 장부

이 문서는 엔진과 예제 게임의 버전별 변경 내용을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)의 분류 방식을 참고하며, 버전 번호는 유의적 버전 `MAJOR.MINOR.PATCH`를 사용합니다.

## [Unreleased]

## [0.8.0] - 2026-08-04

### 캐릭터 선택 개선

- 반복되는 대형 캐릭터 카드를 상단 선택 탭과 단일 대형 프로필 구조로 변경
- 선택 캐릭터의 초상화, 역할, 설명, 사격과 폭탄 정보를 한 영역에 집중
- 속도·화력·범위의 5단계 지표와 초기 잔기·폭탄 수 표시
- 캐릭터별 강조색과 역할·선택 지표를 `players.*.selection` JSON 데이터로 분리
- 선택 지표의 1~5 범위 런타임 검증과 JSON Schema 규격 추가

### 정보 밀도와 흐름

- 시작 화면의 로컬 점수판을 JSON의 `leaderboardLimit`에 따라 상위 5개로 제한
- 긴 점수 목록이 캐릭터 선택보다 큰 비중을 차지하던 문제 해결
- 시작 버튼 확정 후 280ms 강조·페이드 전환을 거쳐 인게임에 진입
- 선택 탭에 `aria-pressed` 상태를 제공하고 사용자 데이터 문자열을 HTML로 직접 삽입하지 않도록 구성

## [0.7.3] - 2026-08-04

### UI 일관성

- 캐릭터 선택 화면을 인게임과 동일한 960px 캐비닛 규격으로 통합
- 왼쪽 640px 캐릭터 선택 영역과 오른쪽 320px 키 가이드·점수 패널 구조 적용
- 선택 화면의 콘텐츠 높이를 인게임 플레이 필드와 동일한 720px 기준으로 정렬
- 별도 랜딩 페이지처럼 보이던 대형 안내 제목을 게임 브랜드 헤더로 변경
- 인게임과 동일한 배경, 격자, 패널 그라데이션, 테두리, 모서리, 강조색 사용
- 캐릭터 선택 상태를 인게임 HUD의 청록색 강조선과 같은 규칙으로 통일
- 게임 시작 버튼, 키 가이드, 로컬 점수판의 간격과 글자 크기를 인게임 패널 기준으로 조정
- 모바일에서는 동일 구성요소를 한 열로 쌓되 하나의 연속된 캐비닛처럼 표시

## [0.7.2] - 2026-08-04

### UI 정렬

- 외곽 캐비닛 너비를 실제 게임 규격인 플레이 필드 640px + 상태 패널 320px에 일치
- 캐비닛 내부만 축소되어 좌우에 검은 빈 띠가 생기던 그리드 정렬 문제 수정
- 헤더와 게임 본체가 동일한 960px 기준선에 맞도록 정렬
- 좁은 데스크톱 구간에서도 플레이 필드와 상태 패널이 2:1에 가깝게 함께 축소되도록 수정

### 배경

- 16:9 배경을 8:9 플레이 필드에 강제로 늘려 왜곡하던 렌더링 제거
- 원본 비율을 유지하며 필드를 채우는 `cover` 렌더링 적용
- 스테이지별 `backgroundPresentation.fit`, `focusX`, `focusY`, `alpha` 프로토콜과 검증 추가
- 배경 위치를 흔들던 장식용 상하 트윈을 제거해 필드 경계와 배경 정렬 안정화

## [0.7.1] - 2026-08-04

### UI

- 단판형 게임에 불필요한 진행 저장, 이어하기, 게임 JSON 가져오기·내보내기 UI 제거
- 현재 점수를 주 정보로, 최고 점수를 보조 정보로 재배치
- 플레이 화면 중심의 표준 640px 필드와 280~320px 상태 패널 비율로 정렬
- 사이드 패널 제목, 자원, 그레이즈·체인, 조작 안내의 정보 위계와 간격 정리
- 대화창 크기와 하단 키 안내·다음 버튼 정렬 개선
- 데스크톱·태블릿·모바일 분기점과 좁은 화면 여백 개선

### 동작

- `persistence`가 선언된 데이터셋에서만 자동 체크포인트 타이머를 실행
- 현재 프로토타입에서는 최고 점수 외 진행 상태를 로컬스토리지에 저장하지 않음

## [0.7.0] - 2026-08-04

### 프로토콜 v2

- `capabilities` 선언과 엔진 지원 기능 대조 검증 추가
- 전역 단일 `boss`를 `bosses` 레지스트리와 스테이지별 `bossId` 참조로 변경
- 편대, 이동, 플레이어 사격, 폭탄을 `type` 판별 전략으로 변경
- 적 하나가 복수 탄막 `patterns`를 조합할 수 있도록 확장
- `game.entryStage`로 시작 스테이지를 ID 기반 지정
- JSON Schema와 프로토콜 확장 문서 추가

### 다형성 예제

- 직선 사격·공격형 폭탄 캐릭터와 부채꼴 사격·방어형 폭탄 캐릭터를 동일 엔진에서 데이터만으로 선택 가능
- `line`, `v`, 좌우 `sweep` 편대 및 `linear`, `sine`, `stationary` 이동 실행기
- 보스 레지스트리와 페이즈별 복수 탄막 조합

### 저장 및 검증

- 저장 상태에 보스 정의 ID와 적별 복수 탄막 타이머를 보존
- 캐릭터·스테이지·적·보스·에셋 교차 참조와 전략별 필드 검증 강화
- 프로토콜 v1은 호환되지 않는 문서로 명확히 거부하고 v2 데이터만 실행

## [0.6.1] - 2026-08-04

### UI

- 기본 대화창에서 `1 / 3` 형식의 진행 숫자를 제거
- `ui.dialogue.showProgress`가 `true`일 때만 진행 숫자 요소를 동적으로 추가하도록 데이터화
- 불필요한 오른쪽 여백을 없애고 대사 본문과 하단 조작 안내·버튼 영역을 정렬

## [0.6.0] - 2026-08-04

### 추가

- 콘텐츠 정의와 `runtime` 진행 상태를 한 JSON 뼈대에 결합하는 단일 게임 문서 저장 규격
- 5초 자동저장, 수동 저장, 브라우저 비활성화 시 저장
- 로컬스토리지 이어하기와 게임 JSON 내보내기·불러오기
- 활성 적, 탄환, 아이템, 보스 페이즈와 상대 타이머 복원

### 안정성

- 대화나 화면·보스 페이즈 전환 중에는 불완전한 저장 생성을 보류
- 콘텐츠 프로토콜, 캐릭터·스테이지 ID, 런타임 스키마 검증

다음 버전에 포함할 작업을 이곳에 먼저 기록합니다.

### 예정

- 커스텀 스프라이트 시트와 애니메이션 프로토콜
- 배경 음악과 효과음 데이터 규격
- 탄환 텍스처 및 추가 이동 패턴 데이터화
- 현재 게임 데이터가 참조하는 에셋만 배포하는 패키징 최적화

## [0.5.4] - 2026-08-04

### 수정

- 보스 생성 후 첫 페이즈 초기화 전 플레이어 탄환이 충돌하면 존재하지 않는 페이즈의 `durationMs`를 읽어 게임이 중단되던 문제

### 변경

- 보스 등장 연출 시작부터 첫 페이즈가 준비될 때까지 피해 판정 잠금
- 보스 생성 시 이전에 풀에서 사용하던 일반 적 정의와 체력 상태 제거
- 보스 페이즈 ID가 유효하지 않으면 피해 및 페이즈 종료 처리를 수행하지 않도록 방어
- `finishBossPhase`에서 실제 페이즈 존재 여부를 확인한 뒤 점수와 제한 시간을 계산

### 원인 확인

- 브라우저 콘솔의 `phase is undefined` 및 `finishBossPhase` 스택 추적으로 확정
- AudioContext 자동 시작 및 WebGL 텍스처 메시지는 정지 원인과 무관한 브라우저 경고로 확인

## [0.5.3] - 2026-08-04

### 수정

- 보스 조우 대화의 마지막 입력이 중복 처리되면 보스가 두 번 생성되어 런타임이 중단될 수 있던 문제
- 중복 생성된 보스가 일반 적 처리 경로로 들어가 적 정의가 없는 상태에서 예외를 발생시킬 수 있던 문제
- 보스 생성 시 물리 월드가 이전 대화 정지 상태를 유지할 가능성

### 변경

- UI 대화 종료와 엔진 대화 완료 콜백을 각각 한 번만 실행하도록 멱등 처리
- 보스가 이미 활성화된 경우 추가 생성 요청을 무시
- 정의가 없는 비정상 적 객체는 게임 루프를 중단하지 않고 안전하게 비활성화
- 보스 생성 직전 물리 월드 재개 상태를 방어적으로 확인

### 검증

- 자동화 테스트와 TypeScript 프로덕션 빌드 통과

## [0.5.2] - 2026-08-04

### 수정

- 결과 화면에서 전투 중 누르던 `Z`, `Space`, `Enter` 입력이 즉시 재도전으로 이어져 점수를 확인하지 못할 수 있던 문제

### 변경

- 게임 종료 후에는 재도전 또는 타이틀 복귀 버튼을 직접 클릭해야 다음 화면으로 진행
- 결과 화면 키 안내를 점수 확인 및 버튼 선택 안내로 변경

## [0.5.1] - 2026-08-04

### 수정

- Phaser보다 먼저 등록된 전역 키 리스너가 방향키 입력을 선점해 WASD만 작동하던 문제

### 변경

- 방향키의 브라우저 기본 동작 방지는 Phaser 키보드 캡처 기능이 담당하도록 입력 책임 분리
- 전역 UI 키 리스너는 대화·결과·타이틀 확인키와 Space 기본 동작만 처리하도록 축소

### 검증

- 자동화 테스트와 TypeScript 프로덕션 빌드 통과

## [0.5.0] - 2026-08-03

### 추가

- 게임 로고, 캐릭터 선택, 키 가이드와 게임 시작 버튼을 포함한 타이틀 화면
- JSON `players` 목록과 `game.defaultPlayer` 기반 다중 캐릭터 프로토콜
- 캐릭터별 이름, 설명, 초상화, 이동 능력, 초기 자원, 샷과 폭탄 명칭, 무장 데이터
- 게임별 로컬스토리지 상위 10개 점수표
- 점수, 그레이즈, 클리어 여부, 사용 캐릭터와 기록 시각 저장
- 손상된 점수 저장 데이터를 안전하게 무시하는 로더
- 결과 화면의 같은 캐릭터 재도전 및 타이틀 복귀 버튼
- 점수표 초기화 기능
- 로컬 점수 정렬·제한·초기화 자동화 테스트

### 변경

- 페이지 진입 직후 전투를 시작하지 않고 타이틀에서 선택한 캐릭터 ID를 Scene에 주입하도록 수명주기 변경
- 단일 `player` 데이터 구조를 ID 기반 `players` 데이터셋으로 확장
- 게임 오버 및 클리어 기록을 결과 화면 진입 시 로컬 점수표에 자동 등록

### 검증

- 전체 자동화 테스트 15개 통과
- TypeScript 타입 검사와 Vite 프로덕션 빌드 통과

## [0.4.2] - 2026-08-03

### 수정

- 빠른 플레이어 탄환의 충돌 영역이 지나치게 짧아 낮은 프레임에서 적을 관통할 수 있던 문제
- 적 오브젝트 풀에서 재사용된 스프라이트가 보스 또는 이전 적 텍스처를 유지할 수 있던 문제
- 보스 클리어와 게임 오버 뒤 물리 시뮬레이션이 결과 화면 뒤에서 계속 실행되던 문제
- 대화 또는 결과 화면에서 일시정지 상태가 중첩될 수 있던 문제
- 불완전한 외부 JSON에서 `rules`가 없으면 검증 오류 대신 예외가 발생할 수 있던 문제
- JSON에서 가져온 조작 안내를 HTML 문자열로 삽입하던 부분을 안전한 텍스트 노드 생성 방식으로 변경

### 최적화

- HUD 상태 전송을 초당 60회에서 최대 20회로 제한해 DOM 레이아웃 부하 감소
- 최고 점수를 매 득점마다 동기 저장하지 않고 주기적·종료 시 저장하도록 변경
- 게임 종료 시 물리 월드를 정지해 불필요한 탄환과 적 계산 제거

### 추가

- 실제 에셋 파일 존재 여부를 게임 시작 전에 검사
- 필수 규칙 묶음 누락 시 안전하게 오류를 반환하는 회귀 테스트

### 검증

- 전체 자동화 테스트 12개 통과
- TypeScript 타입 검사와 Vite 프로덕션 빌드 통과
- 프로덕션 의존성 보안 감사 결과 알려진 취약점 0개

## [0.4.1] - 2026-08-03

### 수정

- 재도전 시 이전 판의 종료 상태와 웨이브 인덱스가 남아 적이 등장하지 않던 문제
- 점수, 잔기, 폭탄, 파워, 그레이즈, 보스 페이즈와 탄막 타이머가 새 판에 영향을 주던 문제
- 방향키 입력을 브라우저 스크롤 방지 리스너가 Phaser보다 먼저 가로채던 문제
- 한 프레임에 여러 플레이어 탄환이 보스에게 충돌하면 다음 페이즈 피해까지 중복 적용될 수 있던 문제
- 재시작 시 이전 결과·대화·일시정지·스테이지 메시지가 화면에 남을 수 있던 문제

### 변경

- 매 플레이 시작 시 런타임 상태를 완전히 초기화해 각 판을 독립적으로 실행
- 보스 페이즈 사이에 짧은 전환 잠금을 두어 탄막과 피해 처리를 안정화

### 검증

- 전체 자동화 테스트 및 프로덕션 빌드 통과

## [0.4.0] - 2026-08-03

### 추가

- 대화를 `Z`, `Space`, `Enter`, 마우스 클릭으로 진행하는 조작
- 대화 진행 순서와 입력 안내 표시
- 결과 화면에서 공격키·확인키로 재도전하는 기능
- 브라우저 탭 이탈 시 자동 일시정지
- `rules` 게임 규칙 데이터
- `scoring` 점수 규칙 데이터
- 파워 단계별 플레이어 무장 데이터
- 보스 등장 위치와 이동 궤적 데이터
- ID 기반 다음 스테이지 연결
- UI 라벨, 안내, 시스템 메시지 데이터
- 잘못된 게임 규칙과 스테이지 연결을 검사하는 테스트

### 변경

- 작품마다 바뀔 수 있는 플레이 수치와 문구를 TypeScript에서 `shooter.json`으로 이동
- 엔진은 입력, 물리, 충돌, 탄막 실행과 상태 전환만 담당하도록 역할 정리
- 플레이 화면 크기를 JSON 프로토콜 값으로 생성하도록 변경
- 전투 중 유지한 사격키가 대사를 연속 스킵하지 않도록 입력 처리 개선

### 검증

- 전체 자동화 테스트 11개 통과
- TypeScript 타입 검사 통과
- Vite 프로덕션 빌드 통과

## [0.3.0] - 2026-08-03

### 추가

- 짧은 플레이용 스테이지 2개
- 오프닝, 스테이지 전환, 보스 조우, 클리어 대화
- 대화 중 전투 정지와 종료 후 자동 재개
- 좌우 스탠딩 일러스트 슬롯
- 일러스트 없이도 진행할 수 있는 텍스트 대화
- 보스 셀레네와 2개 스펠 페이즈
- 스테이지 진행 상태 HUD
- 대화·스탠딩·스테이지 참조 검증

### 변경

- 단일 스테이지 구조를 다중 스테이지 프로토콜로 확장
- 대사와 스탠딩 에셋 참조를 `shooter.json`으로 분리

## [0.2.0] - 2026-08-03

### 추가

- Phaser 기반 세로형 탄막 슈팅 런타임
- 플레이어 이동, 집중 이동, 히트박스 표시와 연속 사격
- 파워 아이템과 단계별 다중 사격
- 적 웨이브와 오브젝트 풀
- 조준형, 부채꼴, 방사형, 나선형, 낙하형 탄막
- 잔기, 폭탄, 피격 무적과 게임 오버
- 그레이즈, 체인, 점수와 로컬 최고 점수
- 보스 체력, 제한 시간, 다중 페이즈
- 스테이지 클리어와 재도전
- 키보드와 모바일 터치 조작
- 탄막 콘텐츠 전용 타입과 검증기

### 변경

- 메인 실행 진입점을 2D RPG에서 탄막 슈팅 예제로 전환
- 기존 RPG 코드와 데이터는 향후 스토리·상점 결합을 위해 보존

## [0.1.0] - 2026-08-03

### 기반

- Phaser 3, TypeScript, Vite 기반 데이터 주입형 2D RPG 엔진
- JSON 기반 맵, NPC, 대화, 조건, 액션, 퀘스트, 아이템과 상점
- 실시간 이동과 전투
- 브라우저 자동 저장, 수동 저장과 데이터 검증
- 캐릭터, 몬스터, 초상화와 배경 에셋 매니페스트

## 버전 기록 규칙

- `MAJOR`: 기존 게임 데이터 프로토콜과 호환되지 않는 변경
- `MINOR`: 이전 데이터를 유지하면서 새로운 기능이나 데이터 규격 추가
- `PATCH`: 버그 수정, 밸런스 조정, 문서와 작은 UX 개선
- 코드 변경 시 `package.json`과 `public/game-data/shooter.json`의 버전을 동일하게 갱신합니다.
- 완료한 변경은 `Unreleased`에서 새 버전 항목으로 옮깁니다.
- 각 버전에는 `추가`, `변경`, `수정`, `제거`, `검증` 중 필요한 항목만 기록합니다.
